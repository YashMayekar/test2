import random
import string

# Generate random text
random_text = ''.join(random.choices(string.ascii_letters + string.digits + '\n', k=100000))

# Write to file
with open('/c:/Users/Yash Mayekar/Projects/MLSC/test2/test.txt', 'a') as f:
    f.write(random_text)
    import random
    import string
    import os

    # Create directory if it doesn't exist
    os.makedirs('/c:/Users/Yash Mayekar/Projects/MLSC/test2', exist_ok=True)

    # Generate multiple batches of random text
    file_path = '/c:/Users/Yash Mayekar/Projects/MLSC/test2/test.txt'

    with open(file_path, 'w') as f:
        for batch in range(10):
            random_text = ''.join(random.choices(string.ascii_letters + string.digits + string.punctuation + '\n', k=50000))
            f.write(random_text)
            f.write('\n--- Batch ' + str(batch + 1) + ' ---\n')
            f.write(''.join(random.choices(string.ascii_letters + string.digits, k=100)) + '\n')

    print(f"File created with multiple batches at {file_path}")
    f.write('\n' + ''.join(random.choices(string.ascii_letters, k=50)))

print("File updated with random text")
